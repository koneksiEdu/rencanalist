import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spacepadelbuster.game',
  appName: 'Space Padel Buster',
  webDir: 'dist'
};

export default config;
