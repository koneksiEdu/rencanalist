import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tospacetododge.game',
  appName: 'To Space To Dodge',
  webDir: 'dist'
};

export default config;
