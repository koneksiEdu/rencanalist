import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.snaketherusher.game',
  appName: 'Snake The Rusher',
  webDir: 'dist'
};

export default config;
